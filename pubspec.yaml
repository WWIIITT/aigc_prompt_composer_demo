name: aigc_prompt_composer_demo
description: Android consumer workbench for the aigc_prompt_composer Dart package.
publish_to: none
version: 0.1.0+1

environment:
  sdk: ^3.12.2

dependencies:
  aigc_prompt_composer: 0.1.0-dev.3
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

flutter:
  uses-material-design: true
